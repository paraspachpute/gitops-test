apiVersion: eks.aws.crossplane.io/v1beta1
kind: FargateProfile
metadata:
  name: external-secrets
spec: 
  deletionPolicy: "Delete"
  forProvider: 
    clusterName: -workload
    podExecutionRoleArn: "arn:aws:iam:::role/-workload-external-secrets-fp-exec-role"
    region: 
    selectors:
      - namespace: external-secrets
    subnets: 
      - 
      - 
      - 
  providerConfigRef:
    name: default
