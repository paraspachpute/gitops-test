apiVersion: iam.aws.crossplane.io/v1beta1
kind: Role
metadata:
  name: "uat-workload-external-secrets-fp-exec-role"
spec:
  deletionPolicy: "Delete"
  forProvider:
    assumeRolePolicyDocument: |
      {
          "Version": "2012-10-17",
          "Statement": [
              {
                  "Effect": "Allow",
                  "Principal": {
                      "Service": "eks-fargate-pods.amazonaws.com"
                  },
                  "Action": "sts:AssumeRole"
              }
          ]
      }
    description: "uat-workload external-secrets fargate pod execution role"
    maxSessionDuration: 3600
  providerConfigRef:
    name: default