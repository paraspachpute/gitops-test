apiVersion: eks.aws.crossplane.io/v1beta1
kind: FargateProfile
metadata:
  name: databases
spec: 
  deletionPolicy: "Delete"
  forProvider: 
    clusterName: ENV_NAME-workload
    podExecutionRoleArn: "arn:aws:iam::ACCOUNT_ID:role/ENV_NAME-workload-database-fp-exec-role"
    region: REGION
    selectors:
      - namespace: databases
    subnets: 
      - SUBNET_1
      - SUBNET_2
      - SUBNET_3
  providerConfigRef:
    name: default
