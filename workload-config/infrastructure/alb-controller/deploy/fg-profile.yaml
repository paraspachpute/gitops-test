apiVersion: eks.aws.crossplane.io/v1beta1
kind: FargateProfile
metadata:
  name: alb-controller
spec: 
  deletionPolicy: "Delete"
  forProvider: 
    clusterName: -workload
    podExecutionRoleArn: "arn:aws:iam:::role/-workload-alb-controller-fp-exec-role"
    region: 
    selectors:
      - namespace: alb-controller
    subnets:
      - 
      - 
      - 
  providerConfigRef:
    name: default
