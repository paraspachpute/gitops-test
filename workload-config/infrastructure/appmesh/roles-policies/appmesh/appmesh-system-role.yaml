apiVersion: iam.aws.crossplane.io/v1beta1
kind: Role
metadata:
  name: "-workload-appmesh-sa-role"
spec:
  deletionPolicy: "Delete"
  forProvider:
    assumeRolePolicyDocument: |
      {
          "Version": "2012-10-17",
          "Statement": [
              {
                  "Effect": "Allow",
                  "Principal": {
                      "Federated": "arn:aws:iam:::oidc-provider/oidc.eks..amazonaws.com/id/CLUSTER_OIDC_ID"
                  },
                  "Action": "sts:AssumeRoleWithWebIdentity",
                  "Condition": {
                      "StringEquals": {
                          "oidc.eks..amazonaws.com/id/CLUSTER_OIDC_ID:aud": "sts.amazonaws.com"
                      }
                  }
              }
          ]
      }
    description: "Role to be used by appmesh in -workload cluster"
    maxSessionDuration: 3600
  providerConfigRef:
    name: default  