apiVersion: eks.aws.crossplane.io/v1beta1
kind: FargateProfile
metadata:
  name: appmesh-system
spec: 
  deletionPolicy: "Delete"
  forProvider: 
    clusterName: -workload
    podExecutionRoleArn: "arn:aws:iam:::role/-workload-appmesh-fp-exec-role"
    region: 
    selectors:
      - namespace: appmesh-system
    subnets:
      - 
      - 
      - 
  providerConfigRef:
    name: default
